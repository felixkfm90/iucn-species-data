name: Trigger Vercel Update Species

on:
  schedule:
    - cron: '0 0 1 * *' # 1. jeden Monat 00:00 UTC
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Function
        run: |
          curl -X GET https://vercel.com/felixkfms-projects/iucn_species
